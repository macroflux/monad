name: ci

on:
  pull_request:
  push:
    branches: [main, dev, 'feat/**', 'exp/**']

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Validate JSON Schemas exist
        run: test -f packages/monad-contracts/imu.v1.json
