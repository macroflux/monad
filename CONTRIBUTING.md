# Contributing to MONAD

Use ADRs for consequential decisions. Each contribution is a **molecule** with:
- spec (intent + metric), code, tests, demo, telemetry, docs.

Run CI locally where possible and keep PRs scoped.
